# 油电猫饼维护规则

## 范围与技术基线

- 这是离线 Android 加油记录 App（Compose + Material 3 + Room），正式应用名称为「油电猫饼」，包名 `com.fuellog.app`，最低 API 26、目标/编译 API 35、Java/Kotlin 17。
- 当前正式版本为 `versionName 2.1.0`、`versionCode 26`。每次正式功能发布必须递增 `versionCode`，不得降低；车辆页版本展示继续读取 `BuildConfig.VERSION_NAME`。任何准备生成并保留为正式 Release APK 的代码或可见 UI 修改，无论大小，都必须使用新的版本号和更高的 `versionCode`；小修使用 patch 递增，新增功能使用 minor 递增。
- 仅修改用户明确要求的功能。不要无关重构页面、替换依赖、改变主题/图标/应用名/`applicationId`，或改变既有手势与动画。
- 主页为四方向导航中心：左滑进入当前车辆统计、右滑进入车辆管理、上滑进入历史记录、下滑进入记一笔；车辆统计右滑、车辆管理左滑、记一笔上滑均返回主页。历史记录普通列表滚动优先，只有列表严格位于顶部后重新明确下拉才返回主页；所有页面均保留系统返回；点击主页当前车辆名称使用弹窗编辑当前车辆。
- 四个方向目标页均不显示冗余顶部功能标题，功能名称主要由导航过渡提示表达；新增加油记录功能的用户文案统一使用“记一笔”，内部 FuelRecord 等技术命名不变。
- 页面导航手势与历史记录卡片编辑/删除手势独立：调整主页与侧页导航阈值时，不得改变历史记录的高阈值、方向或 distance-first 行为。

## 数据安全与 Room

- `fuel-log.db` 是用户数据。禁止清库、重建数据库、按名称删除数据、预置或初始化任何车辆/加油记录。
- 首次安装必须保持 `Vehicle = 0`、`FuelRecord = 0`；升级必须保留全部历史数据。
- 当前 Room schema 版本为 3；v1→v2 Migration 给 `vehicles` 新增 `energyType` 并将全部旧 Vehicle 显式迁移为 `FUEL`，v2→v3 Migration 给 `vehicles` 新增可空的 `energyCapacity`。没有确凿必要性不得变更实体、表、字段、外键或 schema；若确需变更，先说明兼容方案并提供 migration，不能以 destructive migration 代替。
- 删除车辆会级联删除其记录；不要改变该行为。车辆按 `createdAt ASC`，记录按 `timestamp ASC, id ASC`，这个稳定排序是业务规则。

## 记录、日期与校验

- 时间戳展示和业务日期均以设备本地时区为准；不得直接把 UTC 日期展示给用户。
- Material `DatePicker` 的值是 UTC 午夜：使用 `datePickerStartMillis()` 转换初始/选择值；用 `timestampWithSelectedDate()` 替换本地日历日期并保留原时间部分。不要另造日期算法。
- 新增记录默认本地当天，只可选择当天或过去日期；UI 选择器和保存层都必须拒绝未来日期。切换日期不得重置已填写的油号、里程、油价、金额或升数。
- 新增、编辑记录都必须验证有限数字、非负里程、正油价与升数，以及金额与 `油价 × 升数` 的误差不超过 `0.02`。
- 任何新增或编辑后，按 `timestamp ASC, id ASC` 的所有记录里程必须严格递增；不允许零/负区间里程。补录历史记录必须先按全量记录校验，不能只与最新记录比较。

## 油耗与首页计算（不可擅改）

- 区间距离 = 当前里程 − 前一条里程；仅距离大于 0 时计算区间油耗 `本次升数 / 距离 × 100`。
- 区间天数使用两条记录的本地 `LocalDate` 差值，最小为 0。
- 总平均油耗以首条为里程基准：`第 2 条及以后升数总和 / (末条里程 − 首条里程) × 100`；首条升数不计入。
- 首页“距上次”与历史记录“距上次”语义不同，不得混用：首页为当前设备本地日期 − 最新一条记录本地日期；历史记录页为当前记录本地日期 − 前一条记录本地日期。两者均基于设备本地 `LocalDate`，历史记录区间天数最小为 0。
- 首页“距上次”必须根据当前设备日期动态计算，不能缓存为某条 `FuelRecord` 的固定历史区间值；补录、编辑、删除后应根据实际最新记录重新派生首页数据。
- 最近一次加油、首页“距上次”、以及 92/95 最近油价都由记录时间顺序派生。补录、编辑、删除后必须让 Room Flow 按当前排序重新计算，不可把补录记录当成最新记录。
- 车辆统计只使用当前 Vehicle 的 FuelRecord：累计记录里程为最新 odometer − 最早 odometer，累计 liters / amount 包含全部记录；平均间隔天数使用相邻 FuelRecord 本地 LocalDate 日期差（同日 0 天也参与）的中位数，最终四舍五入为整数天；平均间隔里程使用有效正相邻里程差的中位数，最终四舍五入为整数 km；少于 2 条记录或没有有效间隔时均无有效值。真实累计数据、总体平均油耗/电耗仍使用全部有效记录，不做异常值过滤或自动删除；最近油价趋势及平均油价参考值必须按 92/95 分别计算和绘制，最多取最新 10 条，不能混成同一系列。

## 车辆能源类型与双能源语义

- 每个 Vehicle 独立拥有 `FUEL` / `ELECTRIC` energyType；`⛽` 表示 FUEL，`⚡` 表示 ELECTRIC。主页当前车辆名称保持纯文本，不显示能源 Emoji；车辆管理列表和车辆创建/编辑相关 UI 保留名称在前、能源 Emoji 在后。
- 正式 Launcher Icon 使用当前提供的油电猫猫图标；普通、圆形与 Adaptive Icon 资源必须保持一致，不能与应用内 Vehicle 能源 Emoji 混淆。
- 新建 Vehicle 默认 FUEL。0 条记录时允许修改 energyType；只要存在至少 1 条记录，energyType 永久锁定，UI 和数据层都不得允许转换。历史燃油记录不得重新解释为电量。
- 当前 Vehicle.energyType 驱动所有相关业务文案、记录类型和单位。FUEL 使用 92 / 95、L、¥/L、L/100km；ELECTRIC 使用家充 / 公共、kWh、¥/kWh、kWh/100km。
- 内部继续保留 FuelRecord 及其既有字段名；电车补能类型在现有类型字段中明确存储为 `HOME` / `PUBLIC`，不得把 92 / 95 当作电车类型的魔法映射。
- 所有记录、近期价格记忆、历史、首页派生值和车辆统计都必须按 Vehicle 严格隔离；切换当前 Vehicle 后必须按其 energyType 重建相关 UI 草稿与语义，不能残留上一辆车的类型或价格。
- 油耗与电耗使用同构 baseline 算法：第一条记录仅作为里程基准，总平均只累计第 2 条及之后的能源量；累计加油量或累计充电量则包含第一条记录。
- 车辆统计价格趋势按能源体系分别保持两条独立序列：燃油车为 92 / 95，电动车为家充 / 公共；每类平均参考线仅使用当前图中该类数据。

## 能源容量与续航估算

- Vehicle 可选能源容量按车辆独立保存：FUEL 为油箱容量（L），ELECTRIC 为电池可用容量（kWh）；容量只用于续航估算，不得修改或重新解释历史记录。
- 续航估算基于当前车辆有效相邻补能区间的真实能耗，每个样本为 `capacity / intervalConsumption × 100`；最多使用时间上最近的 30 个有效样本。
- 0–2 个有效样本仅显示数据不足，3–5 个仅显示 P50 典型续航，至少 6 个使用 P20 / P50 / P80 对应保守 / 典型 / 理想续航。
- 续航估算表示历史数据推算的满油或满电续航，不是实时剩余续航，不得使用“还能跑”等实时语义。

## 版本历史彩蛋

- 车辆页底部版本文本保持低调、无按钮视觉或点击提示。
- 仅连续快速点击 7 次打开本地版本历史；相邻点击超过 1.2 秒重置。Dialog 打开后应避免触发连点导致立即误关闭；不得显示点击计数、Toast、Snackbar、震动或联网请求。
- 版本历史使用可关闭、可滚动的本地 Dialog；关闭或系统返回后留在车辆页。新增正式版本时同步维护本地版本记录内容。

## 测试、构建与发布

- 改动业务逻辑时补充/更新单元测试。至少运行 `./gradlew testReleaseUnitTest`；发布前运行 `./gradlew testReleaseUnitTest assembleRelease`。
- Release 必须继续使用现有 `.release-signing.properties` 和正式 keystore；绝不生成或替换签名、提交凭据或签名文件。
- Release APK 输出后复制到 `release/`，命名为 `油电猫饼-v<versionName>-release.apk`，并报告 SHA-256。可用 `apksigner verify --verbose --print-certs` 核验签名。
- `release/`、`release-signing/`、`.release-signing.properties`、`local.properties` 和 `.gradle/` 均为本地文件，不纳入版本控制。
